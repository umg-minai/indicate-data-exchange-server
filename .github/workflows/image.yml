name: publish-container-image

on:
  push:
    branches: [ main ]

env:
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - name: Get Python package version
        run: |
          VERSION=$(cat indicate_data_exchange_server/__init__.py | sed -Ee "s/__version__ *= *'(.*)' */\1/")
          echo "PACKAGE_VERSION=${VERSION}" >> "${GITHUB_ENV}"

      - name: Build the Docker image
        run: |
          docker build -t ghcr.io/${{ env.IMAGE_NAME }}:${PACKAGE_VERSION} -f Containerfile .
          docker tag ghcr.io/${{ env.IMAGE_NAME }}:${PACKAGE_VERSION} ghcr.io/${{ env.IMAGE_NAME }}:latest

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push the Docker image
        run: |
          docker push ghcr.io/${{ env.IMAGE_NAME }}:${PACKAGE_VERSION}
          docker push ghcr.io/${{ env.IMAGE_NAME }}:latest
